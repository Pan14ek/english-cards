package ua.nure.englishcards.service.model;

/**
 * A record representing a model for creating a new card topic.
 * Contains the name of the card topic.
 */
public record NewCardTopicModel(String name) {
}
